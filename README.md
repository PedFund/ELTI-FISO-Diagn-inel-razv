# Обработчик диагностики для Vercel

Простое веб-приложение для обработки Excel файлов с диагностикой детей. Деплоится на Vercel.

## 📁 Структура (для Vercel)

```
vercel-app/
├── api/
│   └── index.py          # Весь код + HTML (Flask API)
├── requirements.txt      # Python зависимости
├── vercel.json          # Конфигурация Vercel
└── README.md            # Эта инструкция
```

## 🚀 Деплой на Vercel

### Через GitHub (рекомендуется):

1. **Создайте репозиторий на GitHub**
2. **Загрузите эти 3 файла:**
   - `api/index.py`
   - `requirements.txt`
   - `vercel.json`

3. **Зайдите на https://vercel.com**
4. **New Project → Import Git Repository**
5. **Deploy!**

### Через Vercel CLI:

```bash
npm install -g vercel
vercel login
vercel
```

## 📊 Что делает приложение

1. Загружаете Excel файл через веб-интерфейс
2. Приложение обрабатывает данные (мэппинг + расчёты)
3. Создаёт Excel с 7 листами + 4 графика
4. Автоматически скачивается результат

## 🎯 Формат файла

**Имя:** `{площадка}-{диагностика}-название.xlsx`

**Примеры:**
- ✅ `5-31-Razvitie.xlsx`
- ✅ `12-42-Kidski.xlsx`

## 📦 Результат (7 листов)

1. Возрастные группы + круговая диаграмма
2. Медианы + лепестковая диаграмма
3. Когнитивное развитие + столбчатая
4. Воображение + столбчатая
5. ЭмСоцИнтеллект + столбчатая
6. Населённые пункты
7. Нормированные показатели

## ⚠️ Важно для Vercel

- Все зависимости в `requirements.txt`
- Код в папке `api/` (обязательно!)
- `vercel.json` конфигурирует роутинг
- HTML встроен прямо в Python (нет отдельных шаблонов)

## 🐛 Решение проблем

### Ошибка 404 NOT_FOUND
- ✅ Убедитесь, что файл называется `api/index.py` (не `api.py` или `main.py`)
- ✅ Проверьте `vercel.json` - роуты должны вести на `api/index.py`

### Ошибка при деплое
- ✅ Проверьте `requirements.txt` - все библиотеки указаны?
- ✅ Убедитесь, что используете Python 3.9+

### Ошибка обработки
- ✅ Проверьте формат имени файла: `{площадка}-{диагностика}-*.xlsx`
- ✅ Убедитесь, что в Excel 47 колонок с правильными названиями

## 📞 Поддержка

Если что-то не работает после деплоя:
1. Проверьте логи в Vercel Dashboard
2. Убедитесь, что структура файлов правильная
3. Проверьте, что `api/index.py` содержит весь код

---

**Готово! Деплойте на Vercel!** 🚀
